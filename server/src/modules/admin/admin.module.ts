import { Module } from '@nestjs/common';

/**
 * Admin & Regions (Des §2.3, §4): регионы, города, тарифы, комиссии, провайдеры
 * (payment/sms/maps), feature-флаги, изоляция по region_id, операторская консоль.
 * Реализация — Фаза 7 (Req §7.3–7.5, §6.3).
 */
@Module({})
export class AdminModule {}
