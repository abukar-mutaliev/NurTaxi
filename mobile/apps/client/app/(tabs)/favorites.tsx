import { SavedAddressesScreen } from '@/screens/address';

/** Избранные адреса — вкладка таббара (Figma node 5:36). */
export default function FavoritesRoute() {
  return <SavedAddressesScreen variant="tab" />;
}
