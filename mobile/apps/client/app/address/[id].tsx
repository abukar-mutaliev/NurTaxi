import { SavedAddressDetailScreen } from '@/screens/address';

/** Детальный экран избранного адреса (M3.6). */
export default function SavedAddressDetailRoute() {
  return <SavedAddressDetailScreen />;
}
