apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nurtaxi-backend.fullname" . }}-config
  labels:
    {{- include "nurtaxi-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
