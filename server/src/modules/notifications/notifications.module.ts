import { Module } from '@nestjs/common';

/**
 * Notifications (Des §2.3, §10): push (FCM/APNs), SMS, in-app; шаблоны и настройки;
 * подписка на доменные события. Реализация — Фаза 8 (Req §23).
 */
@Module({})
export class NotificationsModule {}
