# Скопируйте в .env на сервере. Файл .env в git не попадает.
# Локально можно не создавать: docker-compose.yml подставит заглушки только для своей машины.

MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
MINIO_API_BIND=127.0.0.1
MINIO_BROWSER=off
