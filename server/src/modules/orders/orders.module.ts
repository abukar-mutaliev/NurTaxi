import { Module } from '@nestjs/common';

/**
 * Orders & Matching (Des §2.3, §5, §6): заказы, конечный автомат статусов с
 * оптимистичной блокировкой, журнал переходов, подбор водителя (Redis GEO),
 * бизнес-правила (1 активная поездка/заказ).
 * Реализация — Фаза 4 (Req §8.10–8.12, §9, §12).
 */
@Module({})
export class OrdersModule {}
