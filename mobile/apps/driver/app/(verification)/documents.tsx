import { DocumentsScreen } from '@/screens/documents';

/** Загрузка документов (M7.2). Новый маршрут — добавляется в группу (verification). */
export default function DocumentsRoute() {
  return <DocumentsScreen />;
}
