import { Module } from '@nestjs/common';

/**
 * Geo & Routing (Des §2.3, §7): поиск адресов (с учётом специфики адресации СК),
 * расчёт маршрута/ETA/стоимости, гео-индекс через адаптер MapProvider.
 * Реализация — Фаза 3 (Req §8.9, §8.10, §22).
 */
@Module({})
export class GeoModule {}
